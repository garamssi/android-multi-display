<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
      API 29+ variant of the DeskLink shell theme. Same always-dark no-action-bar base
      as the default, plus enforce*Contrast opt-outs so the transparent status and
      navigation bars stay fully transparent under edge-to-edge (enableEdgeToEdge()).
    -->
    <style name="Theme.DeskLink" parent="@android:style/Theme.Material.NoActionBar">
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowActionBar">false</item>
        <item name="android:windowBackground">@color/desklink_bg</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:enforceNavigationBarContrast">false</item>
        <item name="android:enforceStatusBarContrast">false</item>
    </style>
</resources>
