package com.desklink.android.presentation.display

/** Coarse pointer phases the gesture handling understands (mapped from MotionEvent). */
enum class PointerPhase { DOWN, POINTER_DOWN, MOVE, POINTER_UP, UP, CANCEL }
